{
  "name": "expenseformatter",
  "version": "1.0.0",
  "description": "formats mint transaction downloads and removes unecessary entries to be used in calculating monthly expenses",
  "preferGlobal": true,
  "main": "main.js",
  "bin": "src/main.js",
  "scripts": {
    "start": "node -r esm src/main.js",
    "compile": "babel src --out-dir build --copy-files",
    "build:clean": "rm -rf dist && rm -rf build",
    "build": "npm run build:clean && npm run compile && pkg package.json --targets macos-x64 --out-path dist && cp ./node_modules/chokidar/node_modules/fsevents/fsevents.node ./dist/fsevents.node",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.12.1",
    "@babel/core": "^7.12.3",
    "@babel/node": "^7.12.1",
    "@babel/preset-env": "^7.12.1",
    "commander": "^6.1.0",
    "csv": "^5.3.2",
    "csvtojson": "^2.0.10",
    "esm": "^3.2.25",
    "inquirer": "^7.3.3",
    "inquirer-file-path": "^1.0.1",
    "inquirer-select-directory": "^1.2.0",
    "json-2-csv": "^3.7.8"
  }
}
